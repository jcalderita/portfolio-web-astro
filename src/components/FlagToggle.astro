---
interface Props {
  language: string;
}

const { language } = Astro.props;
const targetLanguage = language === "es" ? "en" : "es";
const flag = language === "es" ? "🇺🇸" : "🇪🇸";
const ariaLabel = language === "es" ? "Cambiar a inglés" : "Switch to Spanish";
const title = language === "es" ? "Cambiar el idioma de la web a inglés" : "Switch website language to Spanish";
---

<a href={`/${targetLanguage}/`} class="cursor-pointer text-3xl" title={title} aria-label={ariaLabel}>
  <span aria-hidden="true">{flag}</span>
</a>
